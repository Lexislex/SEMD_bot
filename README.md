# SEMD Bot 🤖

**Телеграм-бот для мониторинга версий СЭМД (Структурированные Электронные Медицинские Документы)**

Этот бот предоставляет актуальную информацию о редакциях СЭМД, их версиях и сроках регистрации в РЭМД (Реестр Электронных Медицинских Документов). Бот автоматически отслеживает обновления справочников ФНСИ и уведомляет пользователей о новых версиях.

## 🔧 Основной функционал

- **Поиск версий СЭМД**: По ID редакции показывает все версии документа с датами начала и окончания регистрации
- **Автоматический мониторинг**: Отслеживает обновления справочников ФНСИ каждые 15 минут
- **Статистика использования**: Команда `/stat` показывает статистику активности бота
- **Уведомления**: Автоматические уведомления об обновлениях для подписанных пользователей
- **База данных**: Ведение логов активности пользователей и истории обновлений

## 🏗️ Архитектура проекта

```
SEMD_bot/
├── main.py                 # Основной файл запуска бота
├── config.py              # Конфигурация и настройки
├── pyproject.toml         # Зависимости проекта (Poetry)
├── handlers/              # Обработчики различной логики
│   ├── fnsi.py           # Работа с API ФНСИ и данными СЭМД
│   ├── scrap.py          # Парсинг и получение обновлений
│   ├── sql.py            # Работа с базой данных
│   └── stat.py           # Генерация статистики
├── utils/                 # Вспомогательные модули
│   ├── data.py           # Список отслеживаемых справочников
│   ├── database.py       # Утилиты для работы с БД
│   ├── file_utils.py     # Работа с файлами
│   ├── logging_setup.py  # Настройка логирования
│   └── text_formatters.py # Форматирование текста
├── env/                   # Конфигурационные файлы
│   ├── data/             # База данных SQLite
│   └── crts/             # SSL сертификаты
├── files/                 # Загруженные файлы справочников
└── logs/                  # Логи приложения
```

## 🚀 Установка и настройка

### Требования

- Python 3.10+
- Poetry для управления зависимостями

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd SEMD_bot
```

2. **Установите зависимости с помощью Poetry:**
```bash
poetry install
```

3. **Создайте файл `.env` в корне проекта:**
```env
# Основные настройки
BOT_TOKEN=your_telegram_bot_token
ENV=production  # development, staging, production
LOG_LEVEL=INFO

# Административные настройки
ADMIN_ID=your_admin_telegram_id
UPDS_MAILING_LIST=chat_id1,chat_id2,chat_id3

# API ФНСИ (если необходимо)
FNSI_API_URL=https://nsi.rosminzdrav.ru/api
FNSI_API_KEY=your_api_key_if_needed
FNSI_FILES_URL=https://nsi.rosminzdrav.ru/files
```

### Получение токена бота

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Следуйте инструкциям и получите токен
4. Добавьте токен в файл `.env`

## 🔧 Конфигурация

Основная конфигурация происходит через переменные окружения в файле `.env`:

### Основные параметры

- `BOT_TOKEN` - токен Telegram бота (обязательно)
- `ENV` - окружение запуска (`development`, `staging`, `production`)
- `LOG_LEVEL` - уровень логирования (`DEBUG`, `INFO`, `WARNING`, `ERROR`)

### Административные настройки

- `ADMIN_ID` - Telegram ID администратора
- `UPDS_MAILING_LIST` - список ID пользователей для рассылки обновлений (через запятую)

### Настройки мониторинга

В режиме `development` проверка обновлений происходит каждую минуту, в `production` - каждые 15 минут.

## 📱 Использование

### Команды бота

- `/start` или `/about` - приветствие и основное меню
- `/stat` - статистика использования бота (для администраторов)

### Интерактивное использование

1. Запустите бота командой `/start`
2. Нажмите кнопку "искать версии СЭМД"
3. Введите ID редакции СЭМД (например: `1234567890`)
4. Получите информацию о всех версиях документа

### Пример вывода

```
Наименование СЭМД: Выписка из медицинской карты стационарного больного
ID    Start     Stop
1234  01.01.20  31.12.20
1235  01.01.21  31.12.21
1236  01.01.22  действует
```

## 🖥️ Запуск

### Локальный запуск

```bash
# Активируйте виртуальное окружение Poetry
poetry shell

# Запустите бота
python main.py
```

### Запуск как системная служба (Linux)

1. **Создайте systemd unit файл:**
```bash
sudo cp env/SEMD_bot.service /etc/systemd/system/
```

2. **Отредактируйте пути в файле службы:**
```bash
sudo nano /etc/systemd/system/SEMD_bot.service
```

3. **Запустите службу:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable SEMD_bot
sudo systemctl start SEMD_bot
```

4. **Проверьте статус:**
```bash
sudo systemctl status SEMD_bot
```

### Запуск в Docker (опционально)

```bash
# Создайте образ
docker build -t semd_bot .

# Запустите контейнер
docker run -d --name semd_bot --env-file .env semd_bot
```

## 📊 Мониторинг

### Логи

Логи приложения сохраняются в файл `logs/semd_bot.log`. Уровень логирования настраивается через переменную `LOG_LEVEL`.

### База данных

Бот использует SQLite для хранения:
- `user_data.sqlite` - данные пользователей и логи активности
- `fnsi_data.sqlite` - информация о справочниках и их версиях

### Статистика

Команда `/stat` показывает:
- Количество пользователей
- Статистику запросов
- Информацию об обновлениях

## 🛠️ Разработка

### Структура кода

- **Обработчики** (`handlers/`) - основная бизнес-логика
- **Утилиты** (`utils/`) - вспомогательные функции
- **Конфигурация** (`config.py`) - централизованные настройки

### Добавление новых справочников

Для мониторинга новых справочников добавьте их OID в список `NSI_LIST` в файле `utils/data.py`.

### Тестирование

```bash
# Запуск тестов
poetry run pytest

# Запуск в режиме разработки
ENV=development python main.py
```

## 🔒 Безопасность

- Все секретные данные хранятся в переменных окружения
- SSL сертификаты размещаются в директории `env/crts/`
- База данных и логи не содержат персональных данных пользователей
- Регулярное обновление зависимостей через Poetry

## 📝 Лицензия

MIT License - подробности в файле `LICENSE`

## 👨‍💻 Автор

**Alexey Alepko** - [AlepkoAA@mos.ru](mailto:AlepkoAA@mos.ru)

## 🤝 Поддержка

Если у вас возникли вопросы или предложения:

1. Создайте Issue в репозитории
2. Напишите автору на email
3. Обратитесь к администратору бота

---

**Версия:** 1.0.0  
**Последнее обновление:** Сентябрь 2025