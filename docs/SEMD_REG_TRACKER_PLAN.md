# План создания плагина semd_reg_tracker

## Описание
Плагин `semd_reg_tracker` - это ScheduledPlugin, который отправляет уведомления в UPDS_MAILING_LIST о СЭМД, которые начинают/прекращают регистрацию в РЭМД.

**Расписание:**
- Первое число каждого месяца в 10:00 MSK - информация о СЭМД за текущий месяц
- Первое число каждого квартала в 10:00 MSK - информация о СЭМД за текущий квартал
- Если 1 число совпадает с началом квартала - отправляется только квартальная сводка

**Dev режим:**
- Ежемесячная проверка каждую минуту
- Ежеквартальная проверка каждые 3 минуты

## 1. Расширение планировщика (core/scheduler.py)

### Текущая поддержка
- `seconds` - секунды
- `minutes` - минуты
- `hours` - часы

### Требуется добавить
- `day` - ежедневно
- `month` - ежемесячно

### Параметр `at`
- Поддержка времени в формате "10:00" для указания конкретного времени запуска
- Конвертация в MSK (московское время)
- Пример: `schedule.every().month.at("10:00").do(func)`

### Вспомогательный метод
- Функция определения первого числа квартала (1 января, 1 апреля, 1 июля, 1 октября)
- Проверка совпадения текущего дня с первым числом месяца/квартала

## 2. Структура плагина plugins/semd_reg_tracker/

```
semd_reg_tracker/
├── __init__.py          # from .plugin import Plugin
├── plugin.py            # ScheduledPlugin класс Plugin
├── handlers.py          # Логика проверки и отправки
├── formatters.py        # Форматирование сообщений
└── README.md            # Документация плагина
```

## 3. Реализация plugin.py (ScheduledPlugin)

### Класс Plugin(ScheduledPlugin)
- `get_name()` - возвращает "SEMDRegTracker"
- `get_version()` - возвращает "1.0.0"
- `initialize()` - инициализация (создание экземпляра handlers)
- `get_scheduled_tasks()` - возвращает две задачи
  1. Ежемесячная: `check_monthly_updates()` - интервал 1 мин (dev) / monthly (prod)
  2. Ежеквартальная: `check_quarterly_updates()` - интервал 3 мин (dev) / quarterly (prod)

### Логика приоритета
Если текущая дата - это первое число квартала (1.01, 1.04, 1.07, 1.10):
- Ежемесячная задача НЕ отправляет уведомление
- Только ежеквартальная задача отправляет (более полную информацию)

### Методы check_monthly_updates и check_quarterly_updates
Оба метода:
1. Получают справочник SEMD1520
2. Фильтруют данные (месячные или квартальные)
3. Форматируют сообщение через SEMDRegistrationFormatter
4. Отправляют в UPDS_MAILING_LIST

## 4. Реализация handlers.py

### Класс SEMDRegistrationHandlers

#### Инициализация
```python
def __init__(self, bot, config):
    self.bot = bot
    self.config = config
    self.semd1520 = None
```

#### Методы

**`_load_semd_data()`**
- Загружает справочник SEMD1520 (как в semd_checker)
- Используется OID = '1.2.643.5.1.13.13.11.1520'
- Возвращает DataFrame с колонками: OID, TYPE, NAME, START_DATE, END_DATE, FORMAT

**`_get_monthly_registrations(year, month)`**
- Фильтрует СЭМД с START_DATE в указанном месяце
- Возвращает DataFrame с поле TYPE и NAME

**`_get_monthly_terminations(year, month)`**
- Фильтрует СЭМД с END_DATE в указанном месяце
- Возвращает DataFrame

**`_get_quarterly_registrations(year, quarter)`**
- Фильтрует СЭМД с START_DATE в указанном квартале
- Возвращает DataFrame

**`_get_quarterly_terminations(year, quarter)`**
- Фильтрует СЭМД с END_DATE в указанном квартале
- Возвращает DataFrame

**`_group_by_date(df, column='START_DATE')`**
- Группирует DataFrame по датам
- Сортирует по датам (asc)
- Внутри каждой группы сортирует по OID
- Возвращает dict: {date: [(oid, name_12chars), ...], ...}

**`send_monthly_update()`**
- Получает текущий месяц и год
- Вызывает методы фильтрации
- Форматирует сообщение
- Отправляет в UPDS_MAILING_LIST

**`send_quarterly_update()`**
- Получает текущий квартал и год
- Вызывает методы фильтрации
- Форматирует сообщение
- Отправляет в UPDS_MAILING_LIST

### Использование Pandas
```python
import pandas as pd

# Месячная фильтрация
monthly_start = df[
    (df['START_DATE'].dt.month == month) &
    (df['START_DATE'].dt.year == year)
]

# Квартальная фильтрация
q_months = [1,2,3] if quarter == 1 else [4,5,6] if quarter == 2 ...
quarterly_start = df[
    (df['START_DATE'].dt.month.isin(q_months)) &
    (df['START_DATE'].dt.year == year)
]

# Группировка и сортировка
grouped = df.groupby(df['START_DATE'].dt.date).apply(
    lambda x: x.sort_values('OID')[['OID', 'NAME']].values.tolist()
)
```

## 5. Реализация formatters.py

### Класс SEMDRegistrationFormatter

#### Уникальные эмодзи (не совпадают с NSI Update Checker)
- 📋 - для начала сообщения/информации
- 🟢 - для начинающих регистрацию
- 🔴 - для прекращающих регистрацию
- 📅 - для дат/заголовков

#### Методы

**`format_monthly(registrations, terminations, year, month)`**
- Возвращает HTML-отформатированное сообщение для месячной сводки

**`format_quarterly(registrations, terminations, year, quarter)`**
- Возвращает HTML-отформатированное сообщение для квартальной сводки

**`_format_date_group(date, semds_list)`**
- Форматирует группу СЭМД на одну дату
- Входные данные: date (datetime), semds_list = [(oid, name_12chars), ...]
- Формат вывода: `• 01.01.2025: OID NAME[:12]`

### Структура HTML сообщения

```
📋 <b>СЭМД - Ежемесячная сводка за [месяц] [год]</b>

🟢 <b>Начинают регистрацию в РЭМД:</b>
• 01.01.2025: 1.2.643.5.1.13... Наименование (мах 12 сим)
• 05.01.2025: 1.2.643.5.1.13... Наименование (мах 12 сим)

🔴 <b>Прекращают регистрацию в РЭМД:</b>
• 15.01.2025: 1.2.643.5.1.13... Наименование (мах 12 сим)
```

### Метод should_send_silent()
- Возвращает False (отправлять со звуком, как в NSI Update Checker)
- Используется в plugin.py при отправке через bot.send_message()

## 6. Регистрация плагина в main.py

Добавить строку:
```python
core.load_plugin('plugins.semd_reg_tracker')
```

Размещение: после загрузки `nsi_update_checker` и перед админскими плагинами.

Пример:
```python
# 2. Публичные плагины
core.load_plugin('plugins.semd_checker')
core.load_plugin('plugins.nsi_update_checker')
core.load_plugin('plugins.semd_reg_tracker')  # <-- НОВАЯ СТРОКА

# 3. Админские плагины
core.load_plugin('plugins.statistics')
```

## 7. Обработка timezone (MSK)

### Проверки
- Уточнить текущую timezone логику в проекте
- Вероятно, используется pytz или datetime.timezone
- Время отправки должно быть 10:00 московского времени (UTC+3)

### Если требуется конвертация
```python
from datetime import datetime
import pytz

msk = pytz.timezone('Europe/Moscow')
now_msk = datetime.now(msk)
```

## 8. Особенности реализации

- **Все СЭМД**: фильтровать без ограничения по FORMAT (FORMAT != 2)
- **Группировка по датам**: сортировка по START_DATE или END_DATE
- **Внутри группы**: сортировка по OID
- **Формат элемента**: `OID + " " + NAME[:12]`
  - Пример: `1.2.643.5.1.13.13.11.1520.1.1 Пациент (СЭ`
- **Отправка**: в каждый chat_id из списка рассылки отдельное сообщение
- **Silent mode**: False (со звуком, как в NSI Update Checker)
- **HTML**: использовать `<b>`, `<i>`, escape спецсимволов

## 9. Логирование

- Использовать logging модуль как в других плагинах
- Логировать:
  - Начало проверки (monthly/quarterly)
  - Количество найденных СЭМД для регистрации
  - Количество найденных СЭМД для завершения
  - Успешные отправки сообщений
  - Ошибки загрузки справочника

## 10. Обработка ошибок

- Try-catch для загрузки справочника SEMD1520
- Try-catch для отправки сообщений в UPDS_MAILING_LIST
- Логирование всех ошибок
- Graceful degradation (если справочник не загрузился - пропустить проверку)

---

**Status**: Plan approved
**Created**: 2025-11-04
**Target**: Implement semd_reg_tracker plugin with monthly and quarterly SEMD registration/termination notifications
