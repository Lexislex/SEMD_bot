#!/bin/bash

# Быстрая очистка FNSI базы данных
# Удаляет базу целиком без подтверждения

set -e

# Определяем путь к корню проекта (scripts/database -> scripts -> SEMD_bot)
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../.." && pwd)"

DB_PATH="$PROJECT_ROOT/env/data/fnsi_data.sqlite"

echo "🗑️  Удаление базы данных FNSI..."
echo "   Путь: $DB_PATH"

if [ -f "$DB_PATH" ]; then
    rm -f "$DB_PATH"
    echo "✅ База данных удалена"
else
    echo "ℹ️  База данных не существует"
fi

echo ""
echo "💡 База будет пересоздана при следующем запуске бота"
echo ""
